"""
core/ — Tiện ích lõi hệ thống Chatbot Tuyển sinh ĐH Vinh.

Exports chính:
    llm.py      → chat_complete(), chat_complete_json()
    embedder.py → embed(), embed_batch(), warmup()
    vectordb.py → search(), upsert()          (Lần 2)
    session.py  → get_history(), save_history() (Lần 2)
"""
